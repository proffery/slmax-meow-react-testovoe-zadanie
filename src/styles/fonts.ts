import { Audiowide, Creepster } from 'next/font/google';

export const audiowide = Audiowide({
    weight: ['400'],
    subsets: ['latin'],
    display: 'swap',
    variable: '--font-audiowide'
});

export const creepster  = Creepster({
    weight: ['400'],
    subsets: ['latin'],
    display: 'swap',
    variable: '--font-creepster'
});